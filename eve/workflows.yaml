version: 1
workflows:
  pm-review:
    trigger:
      system:
        event: doc.ingest
    steps:
      - agent:
          name: pm-coordinator
    hints:
      timeout_seconds: 600
      permission_policy: auto_edit
